fmt.Println(7 / 2) - 3
fmt.Println(7 / 2.0) - 3.5

type MyType = int - алиас
type MyInt int - новый тип

x := 10 - значение внутри функции, автоматом определяется
var x = 10 - значение определяется автоматически
var x int = 10 - значение указывается
var x int - значение указывается, задается стандартное значение (0, false)

switch {
case f < 17:

case f < 21:

default:

}
fallthrough - продолжить проходить через кейсы

for i := 1; i < 5; i++ {
	sum += i
}

for i, ch := range "hello"{
	fmt.Println("индекс", i, "символ", ch)
}

